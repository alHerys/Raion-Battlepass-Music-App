import 'package:flutter/material.dart';

class AppPallete {
  static const yellow = Color(0xFFF0B505);
  static const white = Colors.white;
  static const red = Colors.red;
  static const gray = Colors.grey;
  static const textBlack = Color(0xFF212121);
  static const containerBlack = Color(0xFF262626);
  static const backgroundBlack = Color(0xFF121212);
  static const textGray = Color(0xFF737373);
  static const transparent = Colors.transparent;
}
